releases:
  - name: ctf
    namespace: ctf
    chart: oci://ghcr.io/ctflags/ctf
    version: v1.0.4
    values:
      - values.yaml

#releases:
#  - name: ctf
#    namespace: ctf
#    chart: .
#    values:
#    - values.yaml

